{
  "routes": [
    {
      "method": "GET",
      "path": "/workouts",
      "handler": "Workout.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/workouts/count",
      "handler": "Workout.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/workouts/:_id",
      "handler": "Workout.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/workouts",
      "handler": "Workout.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/workouts/:_id",
      "handler": "Workout.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/workouts/:_id",
      "handler": "Workout.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}