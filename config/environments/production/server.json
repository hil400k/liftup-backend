{
  "host": "${process.env.APP_HOST || 'liftup-backend.herokuapp.com'}",
  "port": "${process.env.NODE_PORT || 1111 }",
  "production": true,
  "proxy": {
    "enabled": false
  },
  "autoReload": {
    "enabled": false
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "autoOpen": false
  }
}
